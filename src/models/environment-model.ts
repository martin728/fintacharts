export interface Environment{
  apiUrl: string;
  provider: string;
  kind: string;
  production: boolean,
  username:string,
  password:string,
  interval:string,
  periodicity:string,
  startDate:string
}
